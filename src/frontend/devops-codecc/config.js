const config = {
  /**
   * code 基础信息
   * checkerset 规则集配置
   * report 通知报告
   * issue 问题提单
   * trigger 扫描触发
   * ignore 路径屏蔽
   * authority 人员权限
   * record 操作记录
   * manage 任务管理
   */
  settings: ['code', 'checkerset', 'trigger', 'ignore', 'record', 'manage'],
}

export default config
